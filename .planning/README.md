# .planning 폴더

이 폴더는 작업 진행 상황을 추적하고 계정 전환 시 작업을 재개하기 위해 사용됩니다.

## 파일 구조

```
.planning/
├── README.md              # 이 파일
├── current_state.md       # 현재 작업 상태 (자동 생성)
├── accounts.md            # 계정별 토큰 사용 현황 (선택)
└── phase_notes/           # Phase별 작업 메모 (선택)
    ├── phase_1_1.md
    ├── phase_1_2.md
    └── ...
```

## 파일 설명

### current_state.md
`/save-state` 스킬 실행 시 자동으로 생성됩니다.
현재 작업 중인 Phase, 수정 중인 파일, 다음 작업 등을 기록합니다.

**생성 시점**:
- 토큰 소진 전 (30,000 토큰 초과 시)
- 계정 전환 전
- 작업 중단 전

**사용 시점**:
- `/resume-work` 스킬 실행 시 자동으로 읽음

### accounts.md (선택)
여러 계정을 사용하는 경우, 각 계정의 토큰 사용 현황을 수동으로 기록합니다.

예시:
```markdown
# 계정 관리

## 계정 1 (joo@example.com)
- 토큰 한도: 40,000
- 현재 사용: 35,200
- 마지막 작업: Phase 1.1
- 상태: 휴식 중

## 계정 2 (joo2@example.com)
- 토큰 한도: 40,000
- 현재 사용: 2,100
- 마지막 작업: -
- 상태: 작업 가능
```

### phase_notes/ (선택)
각 Phase 작업 중 발견한 이슈, 해결 방법, 참고 사항 등을 기록합니다.

예시 (phase_notes/phase_1_1.md):
```markdown
# Phase 1.1 - 헤즈업 게임 규칙

## 작업 일지

### 2026-01-19 14:00
- 헤즈업 감지 로직 구현 시작
- `_is_heads_up()` 메서드 추가
- 기존 `active_players` 카운트 활용

### 2026-01-19 15:30
- 블라인드 포스팅 로직 수정 완료
- 주의: `_post_blinds()` 메서드에서 헤즈업 분기 추가
- 테스트 케이스 3개 작성

## 이슈 및 해결
- 이슈: 2명 → 3명 전환 시 블라인드 위치 오류
- 해결: `_adjust_blinds_on_player_change()` 메서드 추가

## 참고 링크
- PokerKit 문서: https://...
- Robert's Rules: https://...
```

## 사용 방법

### 작업 중단 시
```bash
/save-state
```
→ `current_state.md` 자동 생성

### 작업 재개 시
```bash
/resume-work
```
→ `current_state.md` 자동 읽기

### 진행 상황 확인
```bash
/check-progress
```
→ 전체 현황 출력

## Git 관리

**주의**: 이 폴더의 파일들은 Git에 커밋됩니다.
- `current_state.md`: 커밋 ✅ (계정 간 공유 필요)
- `accounts.md`: 커밋 ❌ (개인 정보, `.gitignore`에 추가)
- `phase_notes/`: 커밋 ✅ (작업 히스토리)

`.gitignore`에 추가 권장:
```
.planning/accounts.md
```

## 자동화

### 자동 저장 (선택)
15분마다 자동으로 상태를 저장하려면:
1. `.claude/hooks/PreToolUse.sh` 생성
2. 다음 코드 추가:
```bash
#!/bin/bash
# 15분(900초)마다 자동 저장
LAST_SAVE_FILE=".planning/.last_save_time"
CURRENT_TIME=$(date +%s)

if [ ! -f "$LAST_SAVE_FILE" ]; then
  echo "0" > "$LAST_SAVE_FILE"
fi

LAST_SAVE=$(cat "$LAST_SAVE_FILE")
ELAPSED=$((CURRENT_TIME - LAST_SAVE))

if [ $ELAPSED -gt 900 ]; then
  echo "⏰ 15분 경과 - 자동 저장 실행"
  echo "$CURRENT_TIME" > "$LAST_SAVE_FILE"
  # /save-state 호출
fi
```

### 세션 시작 시 자동 체크
`.claude/hooks/SessionStart.sh`:
```bash
#!/bin/bash
echo "📊 프로젝트 진행 상황 확인 중..."
# /check-progress --simple 호출
```

## 주의사항

1. **current_state.md는 항상 최신 유지**: 작업 중단 시 반드시 `/save-state` 실행
2. **Git 푸시 필수**: 상태 파일이 푸시되지 않으면 다른 계정에서 접근 불가
3. **민감 정보 제외**: 계정 정보, API 키 등은 기록 금지

## 문제 해결

### current_state.md가 없는 경우
```bash
/check-progress
```
로 마지막 완료 Phase 확인 후 수동으로 시작 위치 결정

### Git 충돌 발생 시
```bash
git pull origin develop
# 충돌 해결
git add .
git commit -m "Merge conflict resolved"
```

### 잘못된 상태 저장 시
```bash
git log .planning/current_state.md
git checkout <commit-hash> .planning/current_state.md
```

## 관련 문서

- [WORK_PLAN_2026.md](../docs/WORK_PLAN_2026.md) - 전체 작업 계획서
- [Skills 가이드](../.claude/skills/) - 사용 가능한 스킬 목록
