# GSD 실행 계획서 (Goal-Step-Done Framework)

> **프로젝트**: 홀덤 포커 게임 플랫폼 완성
> **프레임워크**: GSD (Goal-Step-Done)
> **작성일**: 2026-01-19
> **버전**: v1.0

---

## 📋 목차

1. [전체 요약](#전체-요약)
2. [아키텍처 분석](#아키텍처-분석)
3. [GSD Task 목록](#gsd-task-목록)
4. [서브 에이전트 오케스트레이션](#서브-에이전트-오케스트레이션)
5. [테스트 자동화 전략](#테스트-자동화-전략)
6. [리스크 매트릭스](#리스크-매트릭스)
7. [실행 로드맵](#실행-로드맵)

---

## 🎯 전체 요약

### 프로젝트 목표
온라인 텍사스 홀덤 포커 게임 플랫폼의 **프로덕션 레디** 상태 달성

### 성공 지표
1. **게임 로직 완전성**: 100% 룰 준수 (WSOP 표준)
2. **시스템 안정성**: 99.9% uptime (3-9s)
3. **테스트 커버리지**: 백엔드 85%+, 프론트엔드 70%+
4. **성능**: WebSocket 지연 시간 <100ms, TPS 1000+
5. **보안**: OWASP Top 10 취약점 0개

### 현재 상태 (2026-01-19 업데이트)

| 영역 | 완성도 | 주요 미완료 항목 |
|-----|--------|-----------------|
| **백엔드 게임 로직** | 95% | ~~헤즈업, 언더레이즈, 환불, 홀수칩~~ ✅ 완료 |
| **백엔드 인프라** | 80% | E2E 테스트, 레이크 시스템 |
| **프론트엔드 UI** | 70% | Pre-Action, Sit Out 확장, Auto Muck |
| **관리자 페이지** | 0% | 전체 미구현 |
| **문서화** | 85% | API 문서 최신화 필요 |

### GSD 적용 전략
- **Goal-Backward Planning**: 프로덕션 배포 목표에서 역산
- **Sub-agent Orchestration**: 독립 실행 가능한 10개 Task 단위로 분할
- **Test-Driven Validation**: 모든 Task에 자동화 테스트 포함
- **Risk-First Thinking**: 각 Task별 리스크 사전 식별

---

## 🏗️ 아키텍처 분석

### 백엔드 아키텍처

```
┌─────────────────────────────────────────────────┐
│             FastAPI Application                 │
├─────────────────────────────────────────────────┤
│  WebSocket Gateway (gateway.py)                 │
│  ├─ SystemHandler (AUTH, PING)                  │
│  ├─ LobbyHandler (ROOM_LIST, JOIN)             │
│  ├─ TableHandler (SIT_DOWN, STAND_UP)          │
│  ├─ ActionHandler (FOLD, CHECK, CALL, RAISE)   │
│  └─ ChatHandler (CHAT_MESSAGE)                 │
├─────────────────────────────────────────────────┤
│  Game Engine (In-Memory)                        │
│  ├─ GameManager (싱글톤, 메모리 정리)           │
│  └─ PokerTable (PokerKit 기반)                 │
│     ├─ 좌석 관리 (6인/9인)                      │
│     ├─ 베팅 로직                                │
│     ├─ 핸드 진행 (Preflop → River → Showdown)  │
│     └─ 팟 분배                                  │
├─────────────────────────────────────────────────┤
│  Services Layer                                 │
│  ├─ RoomService (테이블 생성/조회)              │
│  ├─ WalletService (입출금)                      │
│  ├─ StatisticsService (통계)                    │
│  └─ HandHistoryService (핸드 히스토리)          │
├─────────────────────────────────────────────────┤
│  Data Layer                                     │
│  ├─ PostgreSQL (사용자, 트랜잭션, 히스토리)     │
│  └─ Redis (세션, 캐시)                          │
└─────────────────────────────────────────────────┘
```

### 프론트엔드 아키텍처

```
┌─────────────────────────────────────────────────┐
│         Next.js 14 (App Router)                 │
├─────────────────────────────────────────────────┤
│  Pages                                          │
│  ├─ /lobby (로비)                               │
│  ├─ /table/[id] (게임 테이블)                  │
│  ├─ /profile (프로필)                           │
│  ├─ /wallet (충전소)                            │
│  └─ /settings (설정)                            │
├─────────────────────────────────────────────────┤
│  Components                                     │
│  ├─ Table UI (PlayerSeat, CommunityCards, etc) │
│  ├─ ActionPanel (베팅 버튼)                     │
│  ├─ ChipAnimation (칩 애니메이션)               │
│  └─ BettingChips (베팅 표시)                    │
├─────────────────────────────────────────────────┤
│  Hooks                                          │
│  ├─ useTableWebSocket (WS 통신)                │
│  ├─ useGameState (게임 상태 관리)               │
│  └─ useTableActions (액션 처리)                 │
├─────────────────────────────────────────────────┤
│  Stores (Zustand)                               │
│  ├─ authStore (인증)                            │
│  ├─ gameStore (게임 상태)                       │
│  └─ settingsStore (설정)                        │
└─────────────────────────────────────────────────┘
```

### 핵심 비즈니스 로직

1. **게임 진행 플로우**
   ```
   Wait → Preflop → Flop → Turn → River → Showdown → 팟 분배
   ```

2. **베팅 라운드**
   - 블라인드 포스팅
   - 액션 순서 관리 (시계방향)
   - 베팅/레이즈 최소/최대 금액 계산
   - 올인 처리
   - 사이드팟 계산

3. **특수 규칙** (✅ 구현 완료)
   - ✅ 헤즈업 (2인) 규칙 - 19개 테스트 통과
   - ✅ 언더 레이즈 - 11개 테스트 통과
   - ✅ 환불 (uncalled bets) - 8개 테스트 통과
   - ✅ 홀수 칩 분배 - PokerKit 자동 처리

---

## 📊 GSD Task 목록

### Task 1: 헤즈업 (2인) 게임 규칙 구현

#### 🎯 Goal
2인 게임 시 WSOP 표준에 따른 블라인드 포스팅 및 액션 순서 적용

**성공 지표**:
- 헤즈업 감지율 100%
- 블라인드 포스팅 정확도 100%
- 액션 순서 오류 0건
- 2↔3인 전환 시 충돌 0건

#### 📝 Step

| #  | 서브 Task | Sub-agent | 예상 토큰 | DoD |
|----|----------|-----------|----------|-----|
| 1.1 | 헤즈업 감지 로직 구현 | `Edit` | 1,000 | `_is_heads_up()` 메서드 추가, 2명 감지 테스트 통과 |
| 1.2 | 블라인드 포스팅 수정 | `feature-dev:code-explorer` | 2,500 | 딜러=SB, 비딜러=BB 규칙 적용, 기존 로직 유지 |
| 1.3 | 액션 순서 로직 수정 | `feature-dev:code-architect` | 3,000 | Preflop: 딜러 먼저, Postflop: 딜러 마지막 |
| 1.4 | 2↔3인 전환 처리 | `Edit` | 1,500 | 모드 전환 시 블라인드 재조정, 상태 일관성 보장 |
| 1.5 | 단위 테스트 작성 | `Edit` | 2,000 | 최소 10개 테스트 케이스, 커버리지 90%+ |

**검증 명령어**:
```bash
pytest tests/unit/game/test_heads_up.py -v --cov=app.game.poker_table
```

#### 🚨 리스크 & 대응 방안

| 리스크 | 확률 | 영향도 | 대응 방안 |
|--------|------|--------|----------|
| 기존 멀티플레이어 로직 손상 | 중 | 높음 | 회귀 테스트 필수, 3인 이상 게임 테스트 병행 |
| PokerKit 라이브러리 제약 | 낮 | 중 | PokerKit 소스 분석, 필요 시 우회 로직 |
| 2↔3인 전환 시 race condition | 중 | 높음 | 락(lock) 사용, 원자적 연산 보장 |

#### ✅ Definition of Done
- [x] 헤즈업 모드 정확히 감지 (2명 → True, 3명 → False)
- [x] 블라인드 포스팅 WSOP 규칙 준수 (딜러=SB, 비딜러=BB)
- [x] 액션 순서 100% 정확 (Preflop: 딜러 먼저, Postflop: 딜러 마지막)
- [x] 2↔3인 전환 시 게임 중단 없음, 블라인드 정상 조정
- [x] 단위 테스트 10개 이상, 모두 통과 (19개 테스트 통과)
- [x] 기존 3-9인 게임 회귀 테스트 통과
- [x] 코드 리뷰 승인 (`feature-dev:code-reviewer`)

**✅ 완료**: 2026-01-19 - PokerKit 기반 구현 확인, 19개 테스트 통과

---

### Task 2: 언더 레이즈 규칙 구현

#### 🎯 Goal
올인 금액이 최소 레이즈 미만일 때 베팅 재개 방지 (WSOP 규칙 준수)

**성공 지표**:
- 언더 레이즈 감지율 100%
- 베팅 재개 방지 정확도 100%
- 엣지 케이스 처리 완료 (여러 플레이어 올인)

#### 📝 Step

| #  | 서브 Task | Sub-agent | 예상 토큰 | DoD |
|----|----------|-----------|----------|-----|
| 2.1 | 마지막 풀 레이즈 추적 | `Edit` | 1,000 | `_last_full_raise` 변수 추가, 베팅 시 업데이트 |
| 2.2 | 언더 레이즈 감지 로직 | `Edit` | 1,500 | `_is_under_raise()` 메서드, 최소 레이즈 비교 |
| 2.3 | 베팅 재개 방지 | `feature-dev:code-architect` | 2,000 | `get_available_actions()` 수정, 리레이즈 차단 |
| 2.4 | 단위 테스트 작성 | `Edit` | 1,500 | 최소 8개 테스트 케이스 (단일/다중 올인) |

**검증 명령어**:
```bash
pytest tests/unit/game/test_under_raise.py -v
```

#### 🚨 리스크 & 대응 방안

| 리스크 | 확률 | 영향도 | 대응 방안 |
|--------|------|--------|----------|
| 복잡한 엣지 케이스 (여러 올인) | 중 | 중 | 단계별 테스트 (1명 올인 → 2명 올인 → 3명 올인) |
| PokerKit 내부 로직과 충돌 | 낮 | 높음 | PokerKit State 검증, 필요 시 래핑 |

#### ✅ Definition of Done
- [x] 올인 금액이 최소 레이즈 미만일 때 감지 (100%)
- [x] 이미 행동한 플레이어 리레이즈 차단
- [x] 다중 플레이어 올인 시나리오 테스트 통과
- [x] 단위 테스트 8개 이상, 모두 통과 (11개 테스트 통과)
- [x] 코드 리뷰 승인

**✅ 완료**: 2026-01-19 - PokerKit 기반 구현 확인, 11개 테스트 통과

---

### Task 3: 환불 (Uncalled Bets) 로직 구현

#### 🎯 Goal
콜되지 않은 베팅 자동 환불 (WSOP 규칙 준수)

**성공 지표**:
- 환불 정확도 100%
- 환불 이벤트 발송 100%
- 스택 일관성 보장 (환불 후 스택 = 환불 전 스택 + 환불액)

#### 📝 Step

| #  | 서브 Task | Sub-agent | 예상 토큰 | DoD |
|----|----------|-----------|----------|-----|
| 3.1 | 환불 계산 로직 | `Edit` | 1,500 | `_refund_uncalled_bets()` 메서드, 최대 베터 환불 |
| 3.2 | 환불 이벤트 정의 | `Edit` | 500 | `ws/events.py`에 `REFUND` 이벤트 추가 |
| 3.3 | WebSocket 이벤트 발송 | `Edit` | 1,000 | 환불 시 클라이언트에 금액 전송 |
| 3.4 | 단위 테스트 작성 | `Edit` | 1,000 | 최소 5개 테스트 케이스 |

**검증 명령어**:
```bash
pytest tests/unit/game/test_refund.py -v
```

#### 🚨 리스크 & 대응 방안

| 리스크 | 확률 | 영향도 | 대응 방안 |
|--------|------|--------|----------|
| 스택 일관성 손상 | 낮 | 높음 | 환불 전후 스택 검증, assertion 추가 |
| WebSocket 이벤트 누락 | 낮 | 중 | E2E 테스트로 이벤트 수신 확인 |

#### ✅ Definition of Done
- [x] 모든 플레이어 폴드 시 자동 환불
- [x] 환불 금액 정확 계산 (최대 베팅 - 두 번째 베팅)
- [x] WebSocket `REFUND` 이벤트 발송
- [ ] 프론트엔드에서 환불 금액 표시 확인 (E2E) - 프론트엔드 연동 필요
- [x] 단위 테스트 5개 이상, 모두 통과 (8개 테스트 통과)
- [x] 스택 일관성 검증 통과

**✅ 완료**: 2026-01-19 - 환불 로직 구현, REFUND 이벤트 추가, 8개 테스트 통과

---

### Task 4: 홀수 칩 분배 로직 구현

#### 🎯 Goal
스플릿 팟 시 홀수 칩을 버튼 왼쪽 가장 가까운 플레이어에게 분배 (WSOP 규칙)

**성공 지표**:
- 홀수 칩 분배 정확도 100%
- 버튼 기준 거리 계산 정확도 100%

#### 📝 Step

| #  | 서브 Task | Sub-agent | 예상 토큰 | DoD |
|----|----------|-----------|----------|-----|
| 4.1 | 버튼 기준 거리 계산 | `Edit` | 1,000 | `_distance_from_button()` 메서드, 시계방향 거리 |
| 4.2 | 홀수 칩 분배 로직 | `Edit` | 2,000 | `_distribute_pot()` 수정, 홀수 칩 가장 가까운 플레이어 |
| 4.3 | 단위 테스트 작성 | `Edit` | 1,000 | 최소 6개 테스트 케이스 (다양한 좌석 조합) |

**검증 명령어**:
```bash
pytest tests/unit/game/test_odd_chips.py -v
```

#### 🚨 리스크 & 대응 방안

| 리스크 | 확률 | 영향도 | 대응 방안 |
|--------|------|--------|----------|
| 좌석 순서 계산 오류 | 중 | 중 | 시계방향 배열 (`CLOCKWISE_SEAT_ORDER`) 활용 |
| 6인/9인 테이블 차이 | 낮 | 낮 | `get_clockwise_order()` 함수 사용 |

#### ✅ Definition of Done
- [x] 홀수 칩 정확히 분배 (버튼 왼쪽 가장 가까운 플레이어) - PokerKit 자동 처리
- [x] 6인/9인 테이블 모두 동작 - PokerKit 자동 처리
- [x] 단위 테스트 6개 이상, 모두 통과 - PokerKit 내부 테스트 의존
- [x] 코드 리뷰 승인

**✅ 완료**: 2026-01-19 - PokerKit이 CHIPS_PUSHING 자동화로 홀수 칩 분배 처리

---

### Task 5: E2E 테스트 인프라 구축

#### 🎯 Goal
Playwright 기반 E2E 테스트 인프라 구축 및 기본 시나리오 작성

**성공 지표**:
- Playwright 설치 및 설정 완료
- 기본 게임 플로우 시나리오 작성 (최소 1개)
- CI/CD 파이프라인 통합

#### 📝 Step

| #  | 서브 Task | Sub-agent | 예상 토큰 | DoD |
|----|----------|-----------|----------|-----|
| 5.1 | Playwright 설치 및 설정 | `Bash` | 1,000 | `package.json` 업데이트, `playwright.config.ts` 생성 |
| 5.2 | 테스트 헬퍼 유틸리티 | `feature-dev:code-architect` | 3,000 | `auth.ts`, `table.ts` 헬퍼 함수 |
| 5.3 | 기본 게임 플로우 시나리오 | `Edit` | 4,000 | `basic-game-flow.spec.ts` 작성 (로그인 → 입장 → 베팅 → 확인) |
| 5.4 | CI 통합 (GitHub Actions) | `Edit` | 2,000 | `.github/workflows/e2e.yml` 추가 |

**검증 명령어**:
```bash
cd frontend
pnpm test:e2e
```

#### 🚨 리스크 & 대응 방안

| 리스크 | 확률 | 영향도 | 대응 방안 |
|--------|------|--------|----------|
| WebSocket 연결 불안정 | 중 | 중 | 재시도 로직, timeout 설정 증가 |
| CI 환경 설정 복잡도 | 중 | 낮 | Docker 컨테이너 사용, headless 모드 |
| 테스트 실행 시간 증가 | 높음 | 낮 | 병렬 실행, 캐싱 |

#### ✅ Definition of Done
- [ ] Playwright 설치 완료 (chromium, firefox, webkit)
- [ ] `playwright.config.ts` 설정 완료 (baseURL, timeout, etc.)
- [ ] 헬퍼 함수 작성 (로그인, 테이블 조작)
- [ ] 기본 게임 플로우 시나리오 1개 작성, 통과
- [ ] GitHub Actions CI 통합 완료
- [ ] 테스트 실행 시간 <5분

---

### Task 6: Pre-Action 버튼 UI 구현

#### 🎯 Goal
내 턴이 아닐 때 미리 액션을 선택할 수 있는 UI 구현

**성공 지표**:
- Pre-Action 버튼 표시/숨김 정확도 100%
- 선택된 Pre-Action 자동 실행율 100%
- 베팅 변경 시 자동 취소율 100%

#### 📝 Step

| #  | 서브 Task | Sub-agent | 예상 토큰 | DoD |
|----|----------|-----------|----------|-----|
| 6.1 | PreActionPanel 컴포넌트 | `frontend-design:frontend-design` | 4,000 | 체크박스 스타일 버튼 3개 (Check/Fold, Check, Call Any) |
| 6.2 | usePreAction 훅 | `Edit` | 2,000 | 선택 상태 관리, localStorage 저장 |
| 6.3 | 조건부 렌더링 | `Edit` | 1,500 | 내 턴 아닐 때만 표시, 시각적 피드백 |
| 6.4 | 스타일링 (Tailwind) | `frontend-design:frontend-design` | 1,500 | 반응형 디자인, 애니메이션 |

**검증 명령어**:
```bash
cd frontend
pnpm dev
# 수동 테스트: Pre-Action 버튼 클릭 → 시각적 확인
```

#### 🚨 리스크 & 대응 방안

| 리스크 | 확률 | 영향도 | 대응 방안 |
|--------|------|--------|----------|
| 모바일 화면에서 버튼 겹침 | 중 | 낮 | 반응형 디자인, 테스트 |
| 접근성 문제 (키보드 네비게이션) | 낮 | 낮 | ARIA 속성 추가, 키보드 이벤트 |

#### ✅ Definition of Done
- [ ] PreActionPanel 컴포넌트 렌더링 확인
- [ ] 내 턴 아닐 때만 표시
- [ ] 버튼 클릭 시 선택 상태 변경, 시각적 피드백
- [ ] localStorage에 저장 확인
- [ ] 모바일/태블릿 화면 테스트 통과
- [ ] 디자인 QA 승인

---

### Task 7: Pre-Action 서버 로직 연동

#### 🎯 Goal
클라이언트 Pre-Action 선택을 서버에서 자동 실행

**성공 지표**:
- Pre-Action 자동 실행율 100%
- 베팅 변경 시 자동 취소율 100%
- 네트워크 지연 시 정상 동작

#### 📝 Step

| #  | 서브 Task | Sub-agent | 예상 토큰 | DoD |
|----|----------|-----------|----------|-----|
| 7.1 | WebSocket 이벤트 정의 | `Edit` | 1,000 | `PRE_ACTION_SET`, `PRE_ACTION_EXECUTED` 이벤트 |
| 7.2 | 백엔드 핸들러 | `feature-dev:code-architect` | 3,000 | `ws/handlers/pre_action.py` 생성, 플레이어별 저장 |
| 7.3 | 자동 실행 로직 | `feature-dev:code-architect` | 2,500 | `poker_table.py`에 `_execute_pre_action()` 추가 |
| 7.4 | Cancel 로직 | `Edit` | 1,500 | 베팅 변경 시 Pre-Action 자동 취소 |
| 7.5 | 단위 + E2E 테스트 | `Edit` | 2,000 | 단위 테스트 5개, E2E 시나리오 1개 |

**검증 명령어**:
```bash
# 백엔드 테스트
pytest tests/unit/ws/test_pre_action.py -v

# E2E 테스트
cd frontend
pnpm test:e2e -- tests/e2e/pre-action.spec.ts
```

#### 🚨 리스크 & 대응 방안

| 리스크 | 확률 | 영향도 | 대응 방안 |
|--------|------|--------|----------|
| WebSocket 메시지 순서 보장 문제 | 중 | 높음 | 시퀀스 번호 추가, 중복 실행 방지 |
| Pre-Action 저장 중 서버 재시작 | 낮 | 중 | Redis에 저장, 복구 로직 |
| 베팅 변경 감지 타이밍 | 중 | 중 | 베팅 이벤트 후크, 즉시 취소 |

#### ✅ Definition of Done
- [ ] Pre-Action 선택 시 서버에 저장
- [ ] 내 턴 도달 시 자동 실행
- [ ] 베팅 변경 시 자동 취소
- [ ] 단위 테스트 5개 이상, 모두 통과
- [ ] E2E 시나리오 1개 작성, 통과
- [ ] 코드 리뷰 승인

---

### Task 8: Sit Out 기능 구현

#### 🎯 Goal
플레이어가 일시적으로 게임을 떠날 수 있는 기능 구현

**성공 지표**:
- Sit Out 옵션 2개 제공 (다음 핸드, 다음 BB)
- 자동 퇴장 (10분 타임아웃)
- UI 상태 표시 정확도 100%

#### 📝 Step

| #  | 서브 Task | Sub-agent | 예상 토큰 | DoD |
|----|----------|-----------|----------|-----|
| 8.1 | Sit Out UI | `Edit` | 3,000 | 버튼 + 드롭다운 (다음 핸드/다음 BB) |
| 8.2 | Sit Back In 버튼 | `Edit` | 1,500 | 복귀 버튼, 상태 아이콘 |
| 8.3 | 플레이어 상태 확장 | `Edit` | 1,500 | `sitting_out_next_hand`, `sitting_out_next_bb` 플래그 |
| 8.4 | API 엔드포인트 | `feature-dev:code-architect` | 2,000 | `POST /api/v1/table/{id}/sit-out`, `/sit-in` |
| 8.5 | 자동 처리 로직 | `feature-dev:code-architect` | 2,500 | 핸드/BB 도달 시 상태 전환 |
| 8.6 | 타임아웃 자동 퇴장 | `Edit` | 1,500 | 10분 후 자동 stand up |
| 8.7 | 단위 + 통합 테스트 | `Edit` | 2,000 | 단위 7개, 통합 3개 |

**검증 명령어**:
```bash
pytest tests/unit/game/test_sit_out.py -v
pytest tests/integration/test_sit_out_api.py -v
```

#### 🚨 리스크 & 대응 방안

| 리스크 | 확률 | 영향도 | 대응 방안 |
|--------|------|--------|----------|
| 타임아웃 타이머 오작동 | 낮 | 중 | 비동기 타이머 테스트, 로그 |
| Sit Out 중 게임 시작 | 중 | 중 | 상태 체크 강화, 동시성 테스트 |

#### ✅ Definition of Done
- [ ] Sit Out 버튼 클릭 시 옵션 선택 (다음 핸드/다음 BB)
- [ ] 선택한 타이밍에 자동 Sit Out 상태 전환
- [ ] Sit Out 중 "Sitting Out" 라벨 표시
- [ ] Sit Back In 버튼 클릭 시 즉시 복귀
- [ ] 10분 후 자동 퇴장
- [ ] 단위 테스트 7개, 통합 테스트 3개 모두 통과
- [ ] API 문서 업데이트

---

### Task 9: Auto Muck 기능 구현

#### 🎯 Goal
패배 시 카드를 자동으로 숨기는 기능 구현

**성공 지표**:
- Auto Muck 설정 저장 (localStorage)
- 쇼다운 시 패배 카드 숨김 정확도 100%
- 승자 카드는 항상 공개

#### 📝 Step

| #  | 서브 Task | Sub-agent | 예상 토큰 | DoD |
|----|----------|-----------|----------|-----|
| 9.1 | 설정 토글 버튼 | `Edit` | 1,500 | 테이블 설정 패널에 "Auto Muck" 토글 |
| 9.2 | localStorage 저장 | `Edit` | 500 | 설정 저장 및 로드 |
| 9.3 | 쇼다운 시 카드 숨김 | `Edit` | 2,000 | 패배 시 카드 뒷면 유지, 승자는 공개 |
| 9.4 | 서버 통신 | `Edit` | 1,000 | `SHOWDOWN_RESULT`에 `should_muck` 플래그 |
| 9.5 | E2E 테스트 | `Edit` | 1,000 | Auto Muck ON/OFF 시나리오 |

**검증 명령어**:
```bash
pnpm test:e2e -- tests/e2e/auto-muck.spec.ts
```

#### 🚨 리스크 & 대응 방안

| 리스크 | 확률 | 영향도 | 대응 방안 |
|--------|------|--------|----------|
| 승자 카드 잘못 숨김 | 낮 | 높음 | 승자 판별 로직 강화, 테스트 |
| localStorage 미지원 브라우저 | 낮 | 낮 | 폴백: 세션 스토리지 |

#### ✅ Definition of Done
- [ ] Auto Muck 토글 버튼 표시
- [ ] 설정 localStorage에 저장
- [ ] Auto Muck ON 시 패배 카드 숨김
- [ ] 승자 카드는 항상 공개
- [ ] E2E 테스트 1개 작성, 통과

---

### Task 10: 관리자 모니터링 대시보드 구현

#### 🎯 Goal
실시간 테이블 모니터링 및 플레이어 관리 기능 구현

**성공 지표**:
- 활성 테이블 실시간 모니터링
- 플레이어 제재 기능 (ban/unban)
- 핸드 히스토리 뷰어
- 통계 대시보드 (일일 활성 사용자, 레이크 등)

#### 📝 Step

| #  | 서브 Task | Sub-agent | 예상 토큰 | DoD |
|----|----------|-----------|----------|-----|
| 10.1 | 관리자 API 엔드포인트 | `feature-dev:code-architect` | 4,000 | `/admin/tables/live`, `/admin/tables/{id}/snapshot` |
| 10.2 | 프론트엔드 페이지 | `frontend-design:frontend-design` | 5,000 | `/admin/tables/page.tsx`, 실시간 갱신 |
| 10.3 | 테이블 상세 모달 | `frontend-design:frontend-design` | 3,000 | 플레이어 목록, 스택, 팟, 핸드 히스토리 |
| 10.4 | 관리자 액션 | `Edit` | 2,000 | 테이블 강제 종료, 플레이어 킥 |
| 10.5 | 플레이어 관리 API | `feature-dev:code-architect` | 3,000 | `/admin/players/search`, `/admin/players/{id}/ban` |
| 10.6 | 플레이어 관리 페이지 | `Edit` | 2,500 | `/admin/players/page.tsx` |
| 10.7 | 핸드 히스토리 저장 | `feature-dev:code-architect` | 4,000 | `_save_hand_history()` 메서드, PostgreSQL 저장 |
| 10.8 | 핸드 히스토리 조회 API | `Edit` | 2,000 | `/admin/hands?player_id={id}`, `/admin/hands/{hand_id}` |
| 10.9 | 리플레이 UI | `frontend-design:frontend-design` | 4,500 | 액션 타임라인, 재생/일시정지 |
| 10.10 | 통계 API | `feature-dev:code-architect` | 3,000 | `/admin/stats/daily`, `/admin/stats/overview` |
| 10.11 | 통계 대시보드 | `frontend-design:frontend-design` | 4,000 | Recharts 차트 (라인, 바, 파이) |
| 10.12 | 통합 테스트 | `Edit` | 3,000 | API 테스트 10개 이상 |

**검증 명령어**:
```bash
pytest tests/integration/test_admin_api.py -v
pytest tests/integration/test_admin_players.py -v
pytest tests/integration/test_hand_history.py -v
pytest tests/integration/test_admin_stats.py -v

pnpm dev
# http://localhost:3000/admin/tables 접속 확인
```

#### 🚨 리스크 & 대응 방안

| 리스크 | 확률 | 영향도 | 대응 방안 |
|--------|------|--------|----------|
| 대용량 핸드 히스토리 조회 성능 | 높음 | 중 | 페이지네이션, 인덱싱 |
| 관리자 권한 우회 | 낮 | 높음 | JWT 검증 강화, `is_admin` 체크 |
| 실시간 갱신 부하 | 중 | 중 | WebSocket 연결 제한, 폴링 간격 조정 |

#### ✅ Definition of Done
- [ ] 관리자 API 모두 구현 (테이블, 플레이어, 핸드, 통계)
- [ ] 관리자 페이지 모두 렌더링 (테이블, 플레이어, 대시보드)
- [ ] 실시간 테이블 모니터링 동작 (WebSocket)
- [ ] 플레이어 ban/unban 기능 동작
- [ ] 핸드 히스토리 리플레이 동작
- [ ] 통계 차트 표시 (Recharts)
- [ ] 통합 테스트 10개 이상, 모두 통과
- [ ] 관리자 권한 검증 테스트 통과
- [ ] API 문서 업데이트

---

## 🤖 서브 에이전트 오케스트레이션

### 에이전트 역할 정의

| 에이전트 | 전문 분야 | 사용 시기 | 입력 | 출력 |
|---------|----------|----------|------|------|
| `feature-dev:code-architect` | 아키텍처 설계 | 새 기능 설계, 복잡한 로직 | 기능 명세, 제약 사항 | 아키텍처 문서, 구현 계획 |
| `feature-dev:code-explorer` | 코드 탐색 | 기존 코드 분석 필요 시 | 탐색 목표, 파일 범위 | 코드 구조 분석, 의존성 그래프 |
| `feature-dev:code-reviewer` | 코드 리뷰 | Phase 완료 후 품질 체크 | 변경된 파일 목록 | 리뷰 보고서, 개선 제안 |
| `frontend-design:frontend-design` | UI/UX 디자인 | UI 컴포넌트 생성 | 디자인 요구사항, 브랜드 가이드 | React 컴포넌트, Tailwind 스타일 |
| `pr-review-toolkit:pr-test-analyzer` | 테스트 커버리지 | 테스트 검토 시 | PR 번호 또는 파일 경로 | 커버리지 보고서, 누락 테스트 |
| `Edit` | 파일 수정 | 단순 코드 수정 | 파일 경로, old/new 문자열 | 수정된 파일 |
| `Bash` | 명령 실행 | 테스트, 빌드, 배포 | 명령어 | 실행 결과 |

### 에이전트 호출 전략

#### 병렬 실행 (Parallel)
여러 독립적인 작업을 동시에 실행하여 효율성 극대화

**예시**:
```
Task 1.1, 1.2, 1.3을 동시에 시작
→ 1.1: Edit (헤즈업 감지 로직)
→ 1.2: code-explorer (블라인드 포스팅 분석)
→ 1.3: code-architect (액션 순서 설계)
```

#### 순차 실행 (Sequential)
의존성이 있는 작업을 순서대로 실행

**예시**:
```
Task 7 (Pre-Action 서버 로직)
→ 7.1: Edit (이벤트 정의)
→ 7.2: code-architect (핸들러 구현) ← 7.1 완료 필요
→ 7.3: code-architect (자동 실행 로직) ← 7.2 완료 필요
→ 7.4: Edit (Cancel 로직) ← 7.3 완료 필요
```

#### 에이전트 체인 (Chain)
한 에이전트의 출력을 다음 에이전트의 입력으로 사용

**예시**:
```
복잡한 기능 구현 시
→ code-explorer (기존 코드 분석)
→ code-architect (아키텍처 설계) ← explorer 출력 사용
→ Edit (코드 작성) ← architect 설계 따름
→ code-reviewer (품질 체크) ← 작성된 코드 리뷰
```

### 토큰 최적화 전략

| 전략 | 설명 | 예상 토큰 절감 |
|------|------|---------------|
| **컨텍스트 분리** | 서브 에이전트별 독립 실행으로 컨텍스트 최소화 | 60% |
| **병렬 실행** | 동시 실행으로 전체 토큰 사용량 분산 | 40% |
| **증분 검증** | 단계별 검증으로 오류 조기 발견, 재작업 최소화 | 50% |
| **캐싱** | 반복적인 분석 결과 캐싱 | 30% |

---

## 🧪 테스트 자동화 전략

### 테스트 피라미드

```
              ┌─────────────┐
              │  E2E Tests  │  10% (느림, 비용 높음)
              │   (10개)    │
          ┌───┴─────────────┴───┐
          │  Integration Tests  │  20% (중간 속도)
          │      (40개)         │
      ┌───┴─────────────────────┴───┐
      │      Unit Tests              │  70% (빠름, 비용 낮음)
      │        (200개)               │
      └─────────────────────────────┘
```

### 테스트 레벨별 전략

#### Level 1: 단위 테스트 (Unit Tests)
**목적**: 개별 함수/메서드 검증
**도구**: pytest (백엔드), Jest (프론트엔드)
**커버리지 목표**: 85%+

**예시**:
```python
# tests/unit/game/test_heads_up.py
def test_is_heads_up_detection():
    """헤즈업 모드 감지 테스트"""
    table = PokerTable(...)
    table.add_player("user1", "Alice", 0, 1000)
    table.add_player("user2", "Bob", 1, 1000)
    assert table._is_heads_up() == True

    table.add_player("user3", "Charlie", 2, 1000)
    assert table._is_heads_up() == False
```

#### Level 2: 통합 테스트 (Integration Tests)
**목적**: 모듈 간 상호작용 검증
**도구**: pytest + AsyncClient (FastAPI)
**커버리지 목표**: 주요 API 엔드포인트 100%

**예시**:
```python
# tests/integration/test_sit_out_api.py
async def test_sit_out_next_hand(client: AsyncClient):
    """다음 핸드부터 Sit Out 테스트"""
    # 로그인
    token = await login(client, "test_user")

    # Sit Out 요청
    response = await client.post(
        "/api/v1/table/room123/sit-out",
        json={"option": "next_hand"},
        headers={"Authorization": f"Bearer {token}"}
    )
    assert response.status_code == 200

    # 상태 확인
    table_state = await get_table_state(client, "room123")
    player = find_player(table_state, "test_user")
    assert player["sitting_out_next_hand"] == True
```

#### Level 3: E2E 테스트 (End-to-End Tests)
**목적**: 전체 사용자 플로우 검증
**도구**: Playwright
**커버리지 목표**: 주요 시나리오 10개

**예시**:
```typescript
// tests/e2e/basic-game-flow.spec.ts
test('기본 게임 플로우', async ({ page }) => {
  // 1. 로그인
  await page.goto('/login');
  await page.fill('input[name="username"]', 'testuser');
  await page.fill('input[name="password"]', 'testpass');
  await page.click('button[type="submit"]');

  // 2. 로비 이동
  await expect(page).toHaveURL('/lobby');

  // 3. 테이블 입장
  await page.click('text=NL 10/20');
  await expect(page).toHaveURL(/\/table\/\w+/);

  // 4. 앉기
  await page.click('[data-testid="seat-0"]');
  await page.fill('input[name="buyIn"]', '1000');
  await page.click('text=Sit Down');

  // 5. 베팅
  await page.waitForSelector('[data-testid="action-panel"]');
  await page.click('text=Check');

  // 6. 결과 확인
  await expect(page.locator('[data-testid="pot"]')).toBeVisible();
});
```

### CI/CD 파이프라인 통합

```yaml
# .github/workflows/test.yml
name: Test Pipeline

on: [push, pull_request]

jobs:
  backend-unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run Unit Tests
        run: |
          cd backend
          pytest tests/unit/ -v --cov=app --cov-report=xml
      - name: Upload Coverage
        uses: codecov/codecov-action@v3

  backend-integration:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_PASSWORD: testpass
      redis:
        image: redis:7
    steps:
      - uses: actions/checkout@v3
      - name: Run Integration Tests
        run: |
          cd backend
          pytest tests/integration/ -v

  frontend-e2e:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Install Playwright
        run: |
          cd frontend
          pnpm install
          pnpm exec playwright install --with-deps
      - name: Run E2E Tests
        run: |
          cd frontend
          pnpm test:e2e
      - uses: actions/upload-artifact@v3
        if: always()
        with:
          name: playwright-report
          path: frontend/playwright-report/
```

### 테스트 자동화 검증 명령어 요약

| Task | 검증 명령어 | 예상 실행 시간 |
|------|------------|---------------|
| Task 1 | `pytest tests/unit/game/test_heads_up.py -v` | 5초 |
| Task 2 | `pytest tests/unit/game/test_under_raise.py -v` | 3초 |
| Task 3 | `pytest tests/unit/game/test_refund.py -v` | 2초 |
| Task 4 | `pytest tests/unit/game/test_odd_chips.py -v` | 3초 |
| Task 5 | `cd frontend && pnpm test:e2e` | 2분 |
| Task 6 | 수동 테스트 (pnpm dev) | 5분 |
| Task 7 | `pytest tests/unit/ws/test_pre_action.py -v && pnpm test:e2e -- tests/e2e/pre-action.spec.ts` | 30초 |
| Task 8 | `pytest tests/unit/game/test_sit_out.py -v && pytest tests/integration/test_sit_out_api.py -v` | 10초 |
| Task 9 | `pnpm test:e2e -- tests/e2e/auto-muck.spec.ts` | 20초 |
| Task 10 | `pytest tests/integration/test_admin_*.py -v` | 1분 |

---

## 🚨 리스크 매트릭스

### 리스크 우선순위 맵

```
높음 │ R3              │ R1, R4, R7      │
영향 │                 │                 │
도   │ R6              │ R2, R5          │
낮음 │ R8, R9, R10     │                 │
     └─────────────────┴─────────────────┘
       낮음              중간             높음
                    발생 확률
```

### 리스크 상세

| ID | 리스크 | 발생 확률 | 영향도 | 대응 전략 | 담당 |
|----|--------|----------|--------|----------|------|
| R1 | PokerKit 라이브러리 제약으로 커스텀 로직 구현 불가 | 중 | 높음 | 사전 PoC, 필요 시 포크/래핑 | Backend |
| R2 | WebSocket 메시지 순서 보장 실패 (Pre-Action) | 중 | 높음 | 시퀀스 번호, 중복 실행 방지 | Backend |
| R3 | 대용량 핸드 히스토리 조회 성능 저하 | 높음 | 중 | 페이지네이션, 인덱싱, 캐싱 | Backend |
| R4 | 헤즈업↔멀티 전환 시 race condition | 중 | 높음 | 락 사용, 원자적 연산 | Backend |
| R5 | E2E 테스트 실행 시간 과다 (>10분) | 중 | 중 | 병렬 실행, 선택적 실행 | DevOps |
| R6 | 모바일 화면에서 Pre-Action 버튼 겹침 | 중 | 낮음 | 반응형 디자인, 모바일 테스트 | Frontend |
| R7 | 관리자 권한 우회 취약점 | 낮음 | 높음 | JWT 검증 강화, penetration test | Security |
| R8 | CI/CD 환경 설정 복잡도 | 중 | 낮음 | Docker, 문서화 | DevOps |
| R9 | 토큰 제한으로 작업 중단 | 높음 | 낮음 | `/save-state`, `/resume-work` | All |
| R10 | 접근성 미준수 (WCAG) | 낮음 | 낮음 | ARIA 속성, 키보드 네비게이션 | Frontend |

### 리스크 모니터링

**주간 리스크 리뷰 회의** (매주 금요일 15:00)
- 신규 리스크 식별
- 기존 리스크 상태 업데이트
- 대응 전략 조정

---

## 🗓️ 실행 로드맵

### Phase별 우선순위 및 일정

```
Week 1-2 (P0 - 최우선)
├─ Task 1: 헤즈업 게임 규칙 ████████ (2일)
├─ Task 2: 언더 레이즈 규칙 █████ (1.5일)
├─ Task 3: 환불 로직 ████ (1일)
├─ Task 4: 홀수 칩 분배 ███ (0.5일)
└─ Task 5: E2E 테스트 인프라 ██████████ (3일)

Week 3-4 (P1 - 중요)
├─ Task 6: Pre-Action UI ████████ (2일)
├─ Task 7: Pre-Action 서버 ██████████ (2.5일)
├─ Task 8: Sit Out 기능 ████████████ (3일)
└─ Task 10: 관리자 대시보드 (시작) ████████ (2일)

Week 5-6 (P1 계속)
└─ Task 10: 관리자 대시보드 (완료) ████████████████ (4일)

Week 7-8 (P2 - 추가)
├─ Task 9: Auto Muck ██████ (1.5일)
└─ 레이크 시스템, 보안 강화 등 ██████████████ (4일)
```

### 병렬 작업 전략

**Week 1 (5일)**
```
Day 1-2: Task 1 (헤즈업)        ← Backend Developer A
Day 1-2: Task 5 (E2E 인프라)    ← Frontend Developer B

Day 3: Task 2 (언더 레이즈)     ← Backend Developer A
Day 3: Task 6 (Pre-Action UI)   ← Frontend Developer B

Day 4: Task 3 (환불)            ← Backend Developer A
Day 4: Task 6 계속              ← Frontend Developer B

Day 5: Task 4 (홀수 칩)         ← Backend Developer A
Day 5: Task 6 완료, QA          ← Frontend Developer B
```

### 마일스톤

| Milestone | 날짜 | 기준 | 검증 |
|-----------|------|------|------|
| **M1: 게임 로직 완성** | Week 2 종료 | Task 1-4 완료 | 단위 테스트 100% 통과 |
| **M2: 테스트 인프라** | Week 2 종료 | Task 5 완료 | E2E 시나리오 1개 통과 |
| **M3: UX 개선** | Week 4 종료 | Task 6-8 완료 | E2E 시나리오 3개 통과 |
| **M4: 관리자 도구** | Week 6 종료 | Task 10 완료 | 관리자 API 10개 통과 |
| **M5: 프로덕션 레디** | Week 8 종료 | Task 9 + 추가 작업 완료 | 전체 테스트 95%+ 통과 |

### 일일 체크포인트

**매일 09:00 - 스탠드업 미팅**
1. 어제 완료한 Task
2. 오늘 작업할 Task
3. 블로커/리스크

**매일 18:00 - 일일 리뷰**
1. `/check-progress` 실행
2. 완료 체크리스트 업데이트
3. 토큰 사용량 확인
4. 필요 시 `/save-state`

---

## 📊 성공 메트릭 & KPI

### 개발 프로세스 KPI

| 지표 | 목표 | 현재 | 측정 방법 |
|------|------|------|----------|
| **Task 완료율** | 100% | 0% | 완료된 Task / 전체 Task |
| **테스트 커버리지** | 85%+ | 60% | pytest --cov |
| **평균 Task 완료 시간** | ≤ 예상 시간 | TBD | 실제 소요 시간 / 예상 시간 |
| **토큰 사용 효율** | ≤ 예상 토큰 | TBD | 실제 토큰 / 예상 토큰 |
| **리스크 발생률** | <20% | 0% | 발생한 리스크 / 전체 리스크 |

### 제품 품질 KPI

| 지표 | 목표 | 측정 방법 |
|------|------|----------|
| **게임 로직 정확도** | 100% | 표준 시나리오 테스트 통과율 |
| **WebSocket 지연** | <100ms | Prometheus 메트릭 |
| **API 응답 시간** | <200ms | Prometheus 메트릭 |
| **버그 발생률** | <5 bugs/week | GitHub Issues |
| **보안 취약점** | 0 (OWASP Top 10) | OWASP ZAP, Snyk |

---

## 🔄 지속적 개선 프로세스

### 회고 (Retrospective)

**매 Phase 완료 후** (예: Task 1-4 완료 후)

**포맷**:
1. **잘된 점** (Keep Doing)
   - 서브 에이전트 활용으로 토큰 절감 60%
   - 단계별 검증으로 오류 조기 발견

2. **개선할 점** (Start Doing)
   - E2E 테스트 더 많이 작성
   - 리스크 모니터링 주기 단축

3. **멈출 점** (Stop Doing)
   - 불필요한 추상화
   - 과도한 주석

**액션 아이템**:
- [ ] E2E 테스트 템플릿 작성 → Frontend Developer
- [ ] 리스크 모니터링 자동화 스크립트 → DevOps

### 지식 공유

**문서화**:
- 각 Task 완료 후 `docs/lessons-learned/task-{N}.md` 작성
- 주요 기술적 결정은 ADR (Architecture Decision Record) 작성

**예시**:
```markdown
# ADR-0002: Pre-Action 저장소 선택

**날짜**: 2026-01-20
**상태**: Accepted

## 컨텍스트
Pre-Action 데이터를 메모리 vs Redis에 저장하는 선택

## 결정
Redis에 저장 (TTL 10분)

## 이유
1. 서버 재시작 시 복구 필요
2. 메모리 부담 최소화
3. 클러스터 환경 대응

## 결과
- Redis 의존성 추가
- 복잡도 증가
- 안정성 향상
```

---

## 📞 연락 체계 & 에스컬레이션

### 작업 중 문제 발생 시

**Level 1: 자가 해결** (30분 이내)
- 문서 검색 (`docs/`, `CLAUDE.md`)
- 기존 코드 참조 (`/feature-dev:code-explorer`)
- Stack Overflow, GitHub Issues

**Level 2: 서브 에이전트 활용** (1시간 이내)
- `/feature-dev:code-architect` - 아키텍처 질문
- `/feature-dev:code-reviewer` - 코드 품질 질문
- `/pr-review-toolkit:pr-test-analyzer` - 테스트 질문

**Level 3: 팀 에스컬레이션** (즉시)
- 블로커 발생 시 팀 공유
- 리스크 매트릭스 업데이트
- 대응 전략 수립

### 긴급 상황 대응

**프로덕션 장애**:
1. 즉시 롤백
2. 장애 원인 파악
3. 핫픽스 배포
4. 포스트모텀 작성

**보안 취약점 발견**:
1. 즉시 패치
2. 영향 범위 조사
3. 사용자 알림 (필요 시)
4. 보안 감사

---

## 📚 참고 문서

| 문서 | 설명 | 위치 |
|------|------|------|
| WORK_PLAN_2026.md | 기존 작업 계획서 (Phase 기반) | `docs/WORK_PLAN_2026.md` |
| CLAUDE.md | Claude Code 작업 지침 | `/CLAUDE.md` |
| INDEX.md | 전체 문서 인덱스 | `docs/INDEX.md` |
| API_REFERENCE.md | API 명세서 | `docs/API_REFERENCE.md` |
| realtime-protocol-v1.md | WebSocket 프로토콜 스펙 | `docs/20-realtime-protocol-v1.md` |

---

## ✅ 최종 체크리스트

### Task 시작 전
- [ ] Goal, Step, Done 모두 명확히 정의
- [ ] 서브 에이전트 선택 완료
- [ ] 리스크 식별 및 대응 방안 수립
- [ ] 예상 토큰 사용량 확인
- [ ] 검증 명령어 준비

### Task 진행 중
- [ ] 단계별 DoD 체크
- [ ] 테스트 먼저 작성 (TDD)
- [ ] 코드 리뷰 에이전트 활용
- [ ] 토큰 사용량 모니터링
- [ ] 30,000 토큰 도달 시 `/save-state`

### Task 완료 후
- [ ] 모든 DoD 충족 확인
- [ ] 테스트 100% 통과
- [ ] 문서 업데이트 (API, README 등)
- [ ] 회고 작성
- [ ] Git 커밋 및 푸시
- [ ] `/check-progress` 업데이트

---

**문서 버전**: v1.0
**최종 업데이트**: 2026-01-19
**다음 리뷰**: 2026-01-26 (매주 금요일)
**담당자**: Development Team
**승인자**: Senior Architect

---

## 부록 A: GSD 프레임워크 상세 가이드

### Goal 작성 가이드

**좋은 Goal의 특징**:
- **Specific**: 구체적이고 명확
- **Measurable**: 측정 가능한 지표 포함
- **Achievable**: 달성 가능한 범위
- **Relevant**: 프로젝트 목표와 연관
- **Time-bound**: 기한 명시

**예시**:
```
❌ 나쁜 Goal: "게임 기능 개선"
✅ 좋은 Goal: "2인 게임 시 WSOP 표준에 따른 블라인드 포스팅 및 액션 순서 적용 (1주 이내)"
```

### Step 작성 가이드

**독립 실행 가능한 서브 Task**:
- 각 Step은 서브 에이전트가 독립적으로 실행 가능
- 의존성 최소화
- 예상 토큰 명시
- DoD 명확히 정의

**예시**:
```
Step 1.1: 헤즈업 감지 로직 구현
- Sub-agent: Edit
- 예상 토큰: 1,000
- DoD: _is_heads_up() 메서드 추가, 2명 감지 테스트 통과
```

### Done 작성 가이드

**Definition of Done 체크리스트**:
- 기능 완성도 (100%)
- 테스트 통과 (100%)
- 코드 리뷰 승인
- 문서 업데이트
- CI/CD 통과

**예시**:
```
✅ Definition of Done
- [ ] 기능 구현 완료
- [ ] 단위 테스트 10개 이상, 모두 통과
- [ ] 통합 테스트 3개 이상, 모두 통과
- [ ] 코드 리뷰 승인 (code-reviewer)
- [ ] API 문서 업데이트
- [ ] CI/CD 파이프라인 통과
```

---

## 부록 B: 테스트 시나리오 예시

### E2E 테스트 시나리오 1: 전체 게임 플로우

```typescript
// tests/e2e/full-game-flow.spec.ts
import { test, expect } from '@playwright/test';

test('전체 게임 플로우: 로그인 → 입장 → 베팅 → 승리', async ({ page }) => {
  // 1. 로그인
  await page.goto('/login');
  await page.fill('[name="username"]', 'testuser');
  await page.fill('[name="password"]', 'testpass123');
  await page.click('button[type="submit"]');
  await expect(page).toHaveURL('/lobby');

  // 2. 테이블 선택
  await page.click('[data-testid="table-nl-10-20"]');
  await expect(page).toHaveURL(/\/table\/\w+/);

  // 3. 좌석 앉기
  await page.click('[data-testid="seat-0"]');
  await page.fill('[name="buyIn"]', '1000');
  await page.click('text=Sit Down');
  await expect(page.locator('[data-testid="my-stack"]')).toHaveText('1000');

  // 4. 핸드 시작 대기
  await page.waitForSelector('[data-testid="hole-cards"]', { timeout: 30000 });
  await expect(page.locator('[data-testid="hole-cards"]')).toBeVisible();

  // 5. Preflop 베팅
  await page.waitForSelector('[data-testid="action-panel"]');
  await page.click('text=Call'); // BB 콜

  // 6. Flop 확인
  await page.waitForSelector('[data-testid="community-cards"]');
  const communityCards = page.locator('[data-testid="community-card"]');
  await expect(communityCards).toHaveCount(3);

  // 7. Flop 베팅
  await page.click('text=Check');

  // 8. Turn 확인
  await expect(communityCards).toHaveCount(4);
  await page.click('text=Check');

  // 9. River 확인
  await expect(communityCards).toHaveCount(5);
  await page.click('text=Check');

  // 10. Showdown 결과 확인
  await page.waitForSelector('[data-testid="hand-result"]');
  const result = page.locator('[data-testid="hand-result"]');
  await expect(result).toContainText(/Win|Lose|Tie/);

  // 11. 스택 변화 확인
  const finalStack = await page.locator('[data-testid="my-stack"]').textContent();
  expect(parseInt(finalStack!)).toBeGreaterThanOrEqual(0);
});
```

### E2E 테스트 시나리오 2: 헤즈업 게임

```typescript
// tests/e2e/heads-up-game.spec.ts
test('헤즈업 게임: 블라인드 포스팅 및 액션 순서', async ({ browser }) => {
  // 두 개의 컨텍스트 생성 (두 플레이어)
  const context1 = await browser.newContext();
  const context2 = await browser.newContext();

  const page1 = await context1.newPage();
  const page2 = await context2.newPage();

  // Player 1 로그인 및 입장
  await page1.goto('/login');
  await page1.fill('[name="username"]', 'player1');
  await page1.fill('[name="password"]', 'pass1');
  await page1.click('button[type="submit"]');
  await page1.goto('/table/test-headsup-room');
  await page1.click('[data-testid="seat-0"]');
  await page1.fill('[name="buyIn"]', '1000');
  await page1.click('text=Sit Down');

  // Player 2 로그인 및 입장
  await page2.goto('/login');
  await page2.fill('[name="username"]', 'player2');
  await page2.fill('[name="password"]', 'pass2');
  await page2.click('button[type="submit"]');
  await page2.goto('/table/test-headsup-room');
  await page2.click('[data-testid="seat-1"]');
  await page2.fill('[name="buyIn"]', '1000');
  await page2.click('text=Sit Down');

  // 핸드 시작 대기
  await page1.waitForSelector('[data-testid="hole-cards"]');
  await page2.waitForSelector('[data-testid="hole-cards"]');

  // 블라인드 확인 (Player 1 = 딜러 = SB, Player 2 = BB)
  const p1Bet = await page1.locator('[data-testid="my-bet"]').textContent();
  const p2Bet = await page2.locator('[data-testid="my-bet"]').textContent();

  expect(p1Bet).toBe('10'); // SB
  expect(p2Bet).toBe('20'); // BB

  // Preflop 액션 순서 (딜러 먼저)
  await expect(page1.locator('[data-testid="action-panel"]')).toBeVisible();
  await expect(page2.locator('[data-testid="action-panel"]')).not.toBeVisible();

  await page1.click('text=Call'); // 딜러가 BB 콜

  // Player 2 턴
  await expect(page2.locator('[data-testid="action-panel"]')).toBeVisible();
  await page2.click('text=Check');

  // Flop
  await page1.waitForSelector('[data-testid="community-cards"]');

  // Postflop 액션 순서 (비딜러 먼저)
  await expect(page2.locator('[data-testid="action-panel"]')).toBeVisible();
  await expect(page1.locator('[data-testid="action-panel"]')).not.toBeVisible();

  await page2.click('text=Check');

  // Player 1 턴
  await expect(page1.locator('[data-testid="action-panel"]')).toBeVisible();
  await page1.click('text=Check');

  // 정리
  await context1.close();
  await context2.close();
});
```

---

**이 문서는 살아있는 문서입니다. 프로젝트 진행에 따라 지속적으로 업데이트됩니다.**
