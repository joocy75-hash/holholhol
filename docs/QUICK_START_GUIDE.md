# 빠른 시작 가이드 (작업자용)

> 여러 계정으로 작업할 때 사용하는 빠른 참조 가이드입니다.

---

## 🚀 새 계정으로 작업 시작

### 1단계: 저장소 업데이트
```bash
cd /Users/mr.joo/Desktop/holdem
git pull origin develop
```

### 2단계: 진행 상황 확인
```bash
/check-progress --simple
```

### 3단계: 작업 재개 (이전 작업이 있는 경우)
```bash
/resume-work
```

### 4단계: 새 작업 시작 (새 Phase 시작)
```bash
/check-progress
# 다음 Phase 확인 후 시작
```

---

## ⚠️ 토큰 소진 전 (30,000 토큰 초과 시)

### 1단계: 상태 저장
```bash
/save-state
```

### 2단계: 계정 전환
- 현재 계정에서 로그아웃
- 새 계정으로 로그인

### 3단계: 작업 재개
```bash
cd /Users/mr.joo/Desktop/holdem
/resume-work
```

---

## 📋 주요 명령어

| 명령어 | 설명 | 사용 시점 |
|--------|------|----------|
| `/check-progress` | 전체 진행 상황 확인 | 작업 시작 전 |
| `/check-progress -s` | 간단한 진행률만 확인 | 빠른 체크 |
| `/check-progress 1.3` | 특정 Phase 상세 확인 | Phase 시작 전 |
| `/save-state` | 현재 작업 상태 저장 | 토큰 소진 전 |
| `/resume-work` | 이전 작업 재개 | 새 계정 시작 시 |

---

## 📂 주요 파일 위치

| 파일 | 경로 | 설명 |
|------|------|------|
| 작업 계획서 | `docs/WORK_PLAN_2026.md` | 전체 Phase 목록 및 체크리스트 |
| 현재 상태 | `.planning/current_state.md` | 작업 중단 시 자동 생성 |
| 백엔드 코드 | `backend/app/` | Python/FastAPI |
| 프론트엔드 코드 | `frontend/src/` | Next.js/TypeScript |
| 테스트 | `backend/tests/` | Pytest |

---

## ✅ Phase 완료 시 체크리스트

```markdown
[ ] 코드 작성 완료
[ ] 테스트 작성 및 통과
[ ] 린트 에러 0개
[ ] Git 커밋 및 푸시
[ ] WORK_PLAN_2026.md에 ✅ 표시
```

---

## 🔧 테스트 실행

### 백엔드
```bash
cd backend
pytest tests/unit/ -v                    # 유닛 테스트
pytest tests/integration/ -v             # 통합 테스트
pytest --cov=app --cov-report=html       # 커버리지
```

### 프론트엔드
```bash
cd frontend
pnpm test                                # 유닛 테스트
pnpm test:e2e                            # E2E 테스트
pnpm build                               # 빌드 테스트
```

---

## 🐛 문제 해결

### Git 충돌
```bash
git status
git diff
# 충돌 파일 수동 해결 후
git add .
git commit -m "Merge conflict resolved"
```

### current_state.md 없음
```bash
/check-progress
# 마지막 완료 Phase 확인 후 다음 Phase 시작
```

### 테스트 실패
```bash
# 실패한 테스트만 재실행
pytest tests/unit/game/test_heads_up.py::test_specific -v

# 상세 로그 확인
pytest -vv --tb=short
```

---

## 📊 진행률 체크 (일일)

매일 작업 시작 전:
```bash
/check-progress
```

주요 확인 사항:
- [ ] 전체 진행률 (X%)
- [ ] 완료된 Phase 개수
- [ ] 현재 작업 중인 Phase
- [ ] 다음 우선순위 작업

---

## 🎯 우선순위 기억

| 우선순위 | 기간 | Phase |
|---------|------|-------|
| **P0** | 2주 | 핵심 게임 로직, E2E 테스트 |
| **P1** | 4주 | 블라인드 관리, Pre-Action, 관리자 |
| **P2** | 8주 | 레이크, 보안, 오토 머크 |

---

## 💾 백업 전략

### 자동 백업 (Git)
모든 작업은 자동으로 Git에 커밋됩니다.
- `/save-state` 실행 시 자동 커밋
- 브랜치: `develop`

### 수동 백업
중요한 작업 후:
```bash
git add .
git commit -m "Phase X.X 완료"
git push origin develop
```

---

## 🔐 보안 주의사항

### Git에 커밋하지 말 것
- `.env` 파일
- `.planning/accounts.md` (계정 정보)
- API 키, 비밀번호

### 이미 커밋된 경우
```bash
# 파일 삭제 (Git에서만)
git rm --cached .planning/accounts.md

# 커밋
git commit -m "Remove sensitive file"

# 푸시
git push origin develop
```

---

## 📞 도움말

### 문서
- [WORK_PLAN_2026.md](./WORK_PLAN_2026.md) - 상세 작업 계획
- [.planning/README.md](../.planning/README.md) - Planning 폴더 가이드
- [CLAUDE.md](../CLAUDE.md) - 프로젝트 코드 규칙

### Skills
- `/resume-work` - 작업 재개
- `/save-state` - 상태 저장
- `/check-progress` - 진행 상황 확인

---

**버전**: v1.0
**최종 업데이트**: 2026-01-19
